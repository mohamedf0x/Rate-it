/** Shared control styling. The forms repeat the same input a dozen times over, so the
 *  classes live here rather than being pasted — and a focus ring changes in one place. */
export const inputClasses =
  "w-full rounded-lg border border-border bg-surface px-3 py-2.5 text-sm text-ink transition-colors placeholder:text-muted focus:border-brand focus:outline-none";

export const labelClasses = "block text-sm font-medium";

export const hintClasses = "mt-1.5 text-xs text-muted";

export const errorClasses = "text-sm text-pomegranate";
